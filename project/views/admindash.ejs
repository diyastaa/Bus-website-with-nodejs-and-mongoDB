<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>

    <style>
        /* styles.css */

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: url("images/tjadmin2.png"); /* Gambar background */
            background-size: cover; /* Mengisi seluruh area background */
        }

        .admin-dashboard {
            display: grid;
            grid-template-columns: 250px 1fr;
            min-height: 100vh;
        }

        .sidebar {
            background-color: #1F4898; /* Ubah warna background sidebar */
            color: #fff;
            padding: 20px;
        }

        .sidebar h2 {
            margin-bottom: 20px;
            font-size: 1.5rem;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }

        .sidebar ul li {
            margin-bottom: 10px;
        }

        .sidebar ul li a {
            color: #fff;
            text-decoration: none;
            display: block;
            padding: 10px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .sidebar ul li a:hover {
            background-color: #555;
        }

        .sidebar .logout { /* Gaya untuk tombol logout */
            display: block;
            color: #fff;
            text-decoration: none;
            padding: 10px;
            margin-top: 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .sidebar .logout:hover {
            background-color: #555;
        }

        .content {
            padding: 20px;
        }

        @media (max-width: 768px) {
            .admin-dashboard {
                grid-template-columns: 1fr;
            }

            .sidebar {
                width: 100%;
            }
        }

        .logo {
            max-width: 100px; /* Mengatur lebar maksimum logo */
            height: auto; /* Menjaga rasio aspek logo */
            display: block; /* Membuat logo menjadi blok */
            margin: 20px auto; /* Menempatkan logo di tengah */
            background-color: white; /* Memberikan latar belakang putih */
            padding: 5px; /* Menambahkan ruang di sekitar logo */
            border-radius: 5px; /* Mengatur sudut logo */
        }
    </style>
</head>
<body>
    <div class="admin-dashboard">
        <div class="sidebar">
            <img src="images/tjadmin.png" class="logo"> <!-- Logo -->
            <h2>Admin Dashboard</h2>
            <a href="/logout" class="logout">Logout</a>
            <ul>
                <li><a href="/rute">Routes</a></li>
                <li><a href="/userview">Users</a></li>
                <li><a href="/lihatkendala">Comments</a></li>
            </ul>
        </div>
        <div class="content">

        </div>
    </div>
</body>
</html>
